{
  "openapi": "3.1.0",
  "info": {
    "title": "2009scape Wiki API",
    "description": "\nA read-only HTTP API over the 2009scape game data: the things in the game, and the\nlinks between them.\n\n### What you get back\n\nAsk for one entity and you get a page descriptor: the whole page described as data,\nin a single response. Identity, an infobox, sections of attributes, and a first page\nof every set of related entities. Each value arrives with its own label, group,\nformat, unit and `derived` flag, so one renderer can draw a type it has never seen.\n\nTwo habits keep a client working as the data grows:\n\n* **Skip what you do not recognise.** A section carries a `render` word saying how it\n  wants to be laid out. If the word is new to you, leave that section out instead of\n  failing. The same goes for any value of an open vocabulary, which is documented\n  with `x-known-values` in place of an enum.\n* **Do not switch on field names.** Everything needed to display a value travels with\n  the value, so your layout never has to know which fields a type has.\n\n### Versions and caching\n\nEvery answer comes from one immutable build named by its data-version, and every\nresponse repeats that version in the `X-Data-Version` header. A given data-version\nalways answers the same way, so you can cache hard. Add `?v=2026.07.30` to pin a\nrequest to one build. If that build is no longer being served you get a 409 naming\nthe build that is, so recovering is one retry.\n\n### Links\n\nA link to another entity carries `type`, `id`, `slug` and `label`, and never a URL.\nYou build the address, so the same item can live at `/items/dragon-scimitar` on your\nsite and `/wiki/item/4587` on somebody else's.\n",
    "version": "1.1.0"
  },
  "paths": {
    "/health": {
      "get": {
        "tags": [
          "meta"
        ],
        "summary": "Check whether this server can answer",
        "description": "Say the server is up with a readable build open.\n\nA process that failed to open one never answers here at all.",
        "operationId": "health",
        "responses": {
          "200": {
            "description": "The server is up and has data open.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Health"
                }
              }
            }
          }
        }
      }
    },
    "/v1/about": {
      "get": {
        "tags": [
          "meta"
        ],
        "summary": "Find out which build of the data is being served",
        "description": "Describe the build every other answer comes from.\n\n`data_version` is what every response repeats in `X-Data-Version` and what `?v=`\npins to.",
        "operationId": "about",
        "responses": {
          "200": {
            "description": "The manifest of the build currently being served.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Manifest"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/entities/{entity_type}/{ref}": {
      "get": {
        "tags": [
          "entities"
        ],
        "summary": "Get the whole page for one entity",
        "description": "Get everything needed to draw one entity's page: identity, the infobox, the\nattribute sections, and a first page of each set of related entities.",
        "operationId": "entity",
        "parameters": [
          {
            "name": "entity_type",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/EntityType",
              "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
            },
            "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
          },
          {
            "name": "ref",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id.",
              "title": "Ref"
            },
            "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id."
          }
        ],
        "responses": {
          "200": {
            "description": "The page, described as data and ready to render.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PageDescriptor"
                }
              }
            }
          },
          "404": {
            "description": "Nothing published answers to that reference. The code says which case it is: `not_found` means there is no such entity, `not_published` means it exists in this build but is held back. A `not_found` for a name rather than an id also carries `near_names`, the address that answers what the name might have been meant to be.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "308": {
            "description": "That reference is retired. `Location` holds the URL the same entity answers at now. Follow it, and store the new one."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/entities/{entity_type}/{ref}/tooltip": {
      "get": {
        "tags": [
          "entities"
        ],
        "summary": "Get the hover card for one entity",
        "description": "Get identity, one sentence, and the few values the registry marks for hover.",
        "operationId": "tooltip",
        "parameters": [
          {
            "name": "entity_type",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/EntityType",
              "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
            },
            "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
          },
          {
            "name": "ref",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id.",
              "title": "Ref"
            },
            "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id."
          }
        ],
        "responses": {
          "200": {
            "description": "Just enough about the entity to fill a hover card.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Tooltip"
                }
              }
            }
          },
          "404": {
            "description": "Nothing published answers to that reference. The code says which case it is: `not_found` means there is no such entity, `not_published` means it exists in this build but is held back. A `not_found` for a name rather than an id also carries `near_names`, the address that answers what the name might have been meant to be.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "308": {
            "description": "That reference is retired. `Location` holds the URL the same entity answers at now. Follow it, and store the new one."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/entities/{entity_type}/{ref}/rel/{rel}": {
      "get": {
        "tags": [
          "entities"
        ],
        "summary": "Page through one relationship of an entity",
        "description": "Read one more page of a set of related entities the entity page only started.\n\n`direction=reverse` reads the link the other way round.",
        "operationId": "walk",
        "parameters": [
          {
            "name": "entity_type",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/EntityType",
              "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
            },
            "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
          },
          {
            "name": "ref",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id.",
              "title": "Ref"
            },
            "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id."
          },
          {
            "name": "rel",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/RelationshipType",
              "description": "Which relationship to page through. `GET /v1/types` lists the relationships each type takes part in."
            },
            "description": "Which relationship to page through. `GET /v1/types` lists the relationships each type takes part in."
          },
          {
            "name": "direction",
            "in": "query",
            "required": false,
            "schema": {
              "$ref": "#/components/schemas/Direction",
              "description": "Which way to read the relationship. `forward` for what this entity points at, `reverse` for what points at it.",
              "default": "forward"
            },
            "description": "Which way to read the relationship. `forward` for what this entity points at, `reverse` for what points at it."
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end.",
              "default": 0,
              "title": "Offset"
            },
            "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end."
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "maximum": 200,
                  "minimum": 1
                },
                {
                  "type": "null"
                }
              ],
              "description": "How many rows this page holds. Defaults to the width a whole entity page uses for the same relationship, so continuing a list is never narrower than the list you are continuing.",
              "title": "Limit"
            },
            "description": "How many rows this page holds. Defaults to the width a whole entity page uses for the same relationship, so continuing a list is never narrower than the list you are continuing."
          }
        ],
        "responses": {
          "200": {
            "description": "One page of related entities, plus the walk that made it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Block"
                }
              }
            }
          },
          "404": {
            "description": "Nothing published answers to that reference. The code says which case it is: `not_found` means there is no such entity, `not_published` means it exists in this build but is held back. A `not_found` for a name rather than an id also carries `near_names`, the address that answers what the name might have been meant to be.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "308": {
            "description": "That reference is retired. `Location` holds the URL the same entity answers at now. Follow it, and store the new one."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/entities/{entity_type}/{ref}/prices": {
      "get": {
        "tags": [
          "entities"
        ],
        "summary": "Page through what one entity has been worth",
        "description": "Read the weekly market readings recorded for one entity, oldest first.\n\nAnything the market never recorded answers with an empty page rather than a 404.",
        "operationId": "history",
        "parameters": [
          {
            "name": "entity_type",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/EntityType",
              "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
            },
            "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
          },
          {
            "name": "ref",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id.",
              "title": "Ref"
            },
            "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id."
          },
          {
            "name": "since",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Read only from this day onwards, written as `YYYY-MM-DD`. Leave it out to read everything recorded.",
              "title": "Since"
            },
            "description": "Read only from this day onwards, written as `YYYY-MM-DD`. Leave it out to read everything recorded."
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end.",
              "default": 0,
              "title": "Offset"
            },
            "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end."
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "maximum": 200,
                  "minimum": 1
                },
                {
                  "type": "null"
                }
              ],
              "description": "How many rows this page holds. Defaults to the width a whole entity page uses for the same relationship, so continuing a list is never narrower than the list you are continuing.",
              "title": "Limit"
            },
            "description": "How many rows this page holds. Defaults to the width a whole entity page uses for the same relationship, so continuing a list is never narrower than the list you are continuing."
          }
        ],
        "responses": {
          "200": {
            "description": "One page of dated market readings, oldest first.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PricePointPage"
                }
              }
            }
          },
          "404": {
            "description": "Nothing published answers to that reference. The code says which case it is: `not_found` means there is no such entity, `not_published` means it exists in this build but is held back. A `not_found` for a name rather than an id also carries `near_names`, the address that answers what the name might have been meant to be.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "308": {
            "description": "That reference is retired. `Location` holds the URL the same entity answers at now. Follow it, and store the new one."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/entities/{entity_type}/{ref}/resolve": {
      "get": {
        "tags": [
          "entities"
        ],
        "summary": "Check what a reference points at, without being sent there",
        "description": "Ask what a reference points at without being redirected there.\n\nLive, retired, held back and unknown all come back as a 200 with an `outcome`.",
        "operationId": "resolve",
        "parameters": [
          {
            "name": "entity_type",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/EntityType",
              "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
            },
            "description": "Which type of thing to look up, such as `item` or `npc`. `GET /v1/types` lists the types this build serves."
          },
          {
            "name": "ref",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id.",
              "title": "Ref"
            },
            "description": "How to address the entity: its id (`4587`), its slug (`dragon-scimitar`), a slug it used to have, or the token the game source calls it by. A reference made only of digits is always an id."
          }
        ],
        "responses": {
          "200": {
            "description": "What the reference resolves to, stated in the body.",
            "content": {
              "application/json": {
                "schema": {
                  "oneOf": [
                    {
                      "$ref": "#/components/schemas/Present"
                    },
                    {
                      "$ref": "#/components/schemas/Moved"
                    },
                    {
                      "$ref": "#/components/schemas/Hidden"
                    },
                    {
                      "$ref": "#/components/schemas/Missing"
                    }
                  ],
                  "discriminator": {
                    "propertyName": "outcome",
                    "mapping": {
                      "found": "#/components/schemas/Present",
                      "moved": "#/components/schemas/Moved",
                      "hidden": "#/components/schemas/Hidden",
                      "missing": "#/components/schemas/Missing"
                    }
                  },
                  "title": "Response Resolve"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/types": {
      "get": {
        "tags": [
          "discovery"
        ],
        "summary": "List every entity type and how its fields are presented",
        "description": "List every type with its attributes and its relationships, labelled for reading\neach way round.",
        "operationId": "types",
        "responses": {
          "200": {
            "description": "Every type served, with its attributes and relationships.",
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/TypeInfo"
                  },
                  "type": "array",
                  "title": "Response Types"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/types/{entity_type}/entities": {
      "get": {
        "tags": [
          "discovery"
        ],
        "summary": "List all entities of one type, a page at a time",
        "description": "List every published entity of one type, sorted and paged, leaving out\nvariants.",
        "operationId": "listing",
        "parameters": [
          {
            "name": "entity_type",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/EntityType",
              "description": "Which type of thing to list, such as `item` or `quest`. `GET /v1/types` lists the types this build serves."
            },
            "description": "Which type of thing to list, such as `item` or `quest`. `GET /v1/types` lists the types this build serves."
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "How many results one page holds. The response repeats the limit it used, so you never have to assume.",
              "default": 50,
              "title": "Limit"
            },
            "description": "How many results one page holds. The response repeats the limit it used, so you never have to assume."
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end.",
              "default": 0,
              "title": "Offset"
            },
            "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end."
          },
          {
            "name": "order",
            "in": "query",
            "required": false,
            "schema": {
              "$ref": "#/components/schemas/SortOrder",
              "description": "How to sort the listing: `name` for alphabetical, `id` for the game's own numbering.",
              "default": "name"
            },
            "description": "How to sort the listing: `name` for alphabetical, `id` for the game's own numbering."
          }
        ],
        "responses": {
          "200": {
            "description": "One page of the index for that type.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EntitySummaryPage"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/types/{entity_type}/compare": {
      "get": {
        "tags": [
          "discovery"
        ],
        "summary": "List the entities of one type whose stored number answers a question",
        "description": "List the entities of one type picked out by a number they store, paged and\nordered.\n\nAnything not carrying a value being compared or sorted on is left out of the\nanswer and out of the total.",
        "operationId": "compare",
        "parameters": [
          {
            "name": "entity_type",
            "in": "path",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/EntityType",
              "description": "Which type of thing to list, such as `item` or `quest`. `GET /v1/types` lists the types this build serves."
            },
            "description": "Which type of thing to list, such as `item` or `quest`. `GET /v1/types` lists the types this build serves."
          },
          {
            "name": "holds",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Which stored number to put a comparison against, given either as the key `GET /v1/types` publishes for it or as the label it publishes beside it. Anything that type declares in a numeric format can be named here, including one part of a value that packs several numbers, written as `parent.part`.",
              "title": "Holds"
            },
            "description": "Which stored number to put a comparison against, given either as the key `GET /v1/types` publishes for it or as the label it publishes beside it. Anything that type declares in a numeric format can be named here, including one part of a value that packs several numbers, written as `parent.part`."
          },
          {
            "name": "how",
            "in": "query",
            "required": false,
            "schema": {
              "$ref": "#/components/schemas/Comparison",
              "description": "How to measure what is stored against the number given. Defaults to `at_least`, which is what a caller passing only a number usually means.",
              "default": "at_least"
            },
            "description": "How to measure what is stored against the number given. Defaults to `at_least`, which is what a caller passing only a number usually means."
          },
          {
            "name": "number",
            "in": "query",
            "required": false,
            "schema": {
              "type": "number",
              "description": "The number to measure the stored one against.",
              "default": 0.0,
              "title": "Number"
            },
            "description": "The number to measure the stored one against."
          },
          {
            "name": "ordered_by",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Which stored number to sort by, named the same way as the one being compared. Anything not carrying it is left out of the answer, because it is absent rather than smallest.",
              "title": "Ordered By"
            },
            "description": "Which stored number to sort by, named the same way as the one being compared. Anything not carrying it is left out of the answer, because it is absent rather than smallest."
          },
          {
            "name": "descending",
            "in": "query",
            "required": false,
            "schema": {
              "type": "boolean",
              "description": "Sort from the largest down rather than from the smallest up.",
              "default": false,
              "title": "Descending"
            },
            "description": "Sort from the largest down rather than from the smallest up."
          },
          {
            "name": "named",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Keep only the entities called exactly this, matched whole and case sensitively. Many entities share one name, so this is how a caller counts or compares the set behind a single name.",
              "title": "Named"
            },
            "description": "Keep only the entities called exactly this, matched whole and case sensitively. Many entities share one name, so this is how a caller counts or compares the set behind a single name."
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "How many results one page holds. The response repeats the limit it used, so you never have to assume.",
              "default": 50,
              "title": "Limit"
            },
            "description": "How many results one page holds. The response repeats the limit it used, so you never have to assume."
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end.",
              "default": 0,
              "title": "Offset"
            },
            "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end."
          }
        ],
        "responses": {
          "200": {
            "description": "One page of entities, each carrying the values the question was about.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Compared"
                }
              }
            }
          },
          "422": {
            "description": "Nothing declared for that type answers to those words. `GET /v1/types` publishes what each type declares, and anything there formatted as a number can be compared.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/search": {
      "get": {
        "tags": [
          "discovery"
        ],
        "summary": "Search the full text across every type",
        "description": "Search names, alternative names and descriptions across every type, best first\nwith a score.",
        "operationId": "search",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "description": "What to search for, typed the way a reader would type it. Every word is matched as a prefix and all of them have to match, so `drag scim` finds the dragon scimitar. There is no query syntax: quotes, brackets and words like `OR` count as ordinary text.",
              "title": "Q"
            },
            "description": "What to search for, typed the way a reader would type it. Every word is matched as a prefix and all of them have to match, so `drag scim` finds the dragon scimitar. There is no query syntax: quotes, brackets and words like `OR` count as ordinary text."
          },
          {
            "name": "type",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/EntityType"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Limit the answer to certain types of thing. Repeat the parameter for more than one, as in `?type=item&type=npc`. Leave it off to cover every type.",
              "title": "Type"
            },
            "description": "Limit the answer to certain types of thing. Repeat the parameter for more than one, as in `?type=item&type=npc`. Leave it off to cover every type."
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "How many results one page holds. The response repeats the limit it used, so you never have to assume.",
              "default": 50,
              "title": "Limit"
            },
            "description": "How many results one page holds. The response repeats the limit it used, so you never have to assume."
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end.",
              "default": 0,
              "title": "Offset"
            },
            "description": "How many results to skip before this page. To read the next page, pass back the `next_offset` from the last response. It is null once you have reached the end."
          }
        ],
        "responses": {
          "200": {
            "description": "One page of matches, best first.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SearchResultPage"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/find": {
      "get": {
        "tags": [
          "discovery"
        ],
        "summary": "Look up the one entity that goes by this name",
        "description": "Turn a name into the one entity it means, with the ranked runners-up.\n\n`best_match` is null only when nothing matched at all.",
        "operationId": "find",
        "parameters": [
          {
            "name": "name",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "description": "The full name of the single thing you mean, such as `Dragon scimitar`.",
              "title": "Name"
            },
            "description": "The full name of the single thing you mean, such as `Dragon scimitar`."
          },
          {
            "name": "type",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/EntityType"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "Limit the answer to certain types of thing. Repeat the parameter for more than one, as in `?type=item&type=npc`. Leave it off to cover every type.",
              "title": "Type"
            },
            "description": "Limit the answer to certain types of thing. Repeat the parameter for more than one, as in `?type=item&type=npc`. Leave it off to cover every type."
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 200,
              "minimum": 1,
              "description": "How many results one page holds. The response repeats the limit it used, so you never have to assume.",
              "default": 50,
              "title": "Limit"
            },
            "description": "How many results one page holds. The response repeats the limit it used, so you never have to assume."
          }
        ],
        "responses": {
          "200": {
            "description": "The single best match, if there is one, plus the runners-up.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Match"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    },
    "/v1/near-names": {
      "get": {
        "tags": [
          "discovery"
        ],
        "summary": "Ask which real names a misspelt one might have meant",
        "description": "Turn an unmatched name into the real names it may have meant, each carrying\nidentity and nothing else.\n\nAn empty answer means nothing was close, which is an answer rather than a failure.",
        "operationId": "near_names",
        "parameters": [
          {
            "name": "name",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "description": "The name that answered to nothing, exactly as it was typed, such as `dragon scimtar`.",
              "title": "Name"
            },
            "description": "The name that answered to nothing, exactly as it was typed, such as `dragon scimtar`."
          },
          {
            "name": "type",
            "in": "query",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/EntityType",
              "description": "Which sort of thing the name belongs to. Required: `dagon scimitar` and `dagon` are near misses for different things depending on whether an item or an npc was meant, so this question cannot be answered without it. `GET /v1/types` lists the sorts this build serves."
            },
            "description": "Which sort of thing the name belongs to. Required: `dagon scimitar` and `dagon` are near misses for different things depending on whether an item or an npc was meant, so this question cannot be answered without it. `GET /v1/types` lists the sorts this build serves."
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer",
                  "maximum": 25,
                  "minimum": 1
                },
                {
                  "type": "null"
                }
              ],
              "description": "At most how many close names to offer. Deliberately small: this is a question to put to a person, not a listing to read.",
              "title": "Limit"
            },
            "description": "At most how many close names to offer. Deliberately small: this is a question to put to a person, not a listing to read."
          },
          {
            "name": "keep",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "number",
                  "maximum": 1.0,
                  "minimum": 0.0
                },
                {
                  "type": "null"
                }
              ],
              "description": "How close to the best candidate a name has to be to be offered alongside it, as a share of the best score. `1.0` offers only names that matched exactly as well; lower values widen the field.",
              "title": "Keep"
            },
            "description": "How close to the best candidate a name has to be to be offered alongside it, as a share of the best score. `1.0` offers only names that matched exactly as well; lower values widen the field."
          }
        ],
        "responses": {
          "200": {
            "description": "The closest names that exist, best first, or nothing at all when none of them is close enough to be worth offering.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SearchResultPage"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          },
          "401": {
            "description": "No key was sent, or it is not one this service answers."
          },
          "403": {
            "description": "Too many refused requests have come from this address."
          },
          "429": {
            "description": "This key has asked for more than its share; `Retry-After` says when to come back."
          }
        },
        "security": [
          {
            "issued_key": []
          }
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "AttributeFormat": {
        "type": "string",
        "title": "AttributeFormat",
        "description": "How to draw a value: a plain number, an amount of coins, a coordinate. Published as an open string rather than a fixed enum, because new values get added as the data grows. The ones this build knows are listed under `x-known-values`. Treat anything else as a value you cannot render yet, not as an error.",
        "x-known-values": [
          "int",
          "float",
          "bool",
          "text",
          "gp",
          "id",
          "ids",
          "ref",
          "enum",
          "skills",
          "bonuses",
          "absorb",
          "coord",
          "area",
          "rate",
          "texts"
        ],
        "examples": [
          "int"
        ]
      },
      "AttributeGroup": {
        "type": "string",
        "title": "AttributeGroup",
        "description": "Which part of a page an attribute belongs to, such as the overview box. Published as an open string rather than a fixed enum, because new values get added as the data grows. The ones this build knows are listed under `x-known-values`. Treat anything else as a value you cannot render yet, not as an error.",
        "x-known-values": [
          "overview",
          "general",
          "trade",
          "equipment",
          "combat",
          "behaviour",
          "drops",
          "map",
          "shop",
          "rate",
          "amount",
          "reward",
          "skill",
          "slayer",
          "internal"
        ],
        "examples": [
          "overview"
        ]
      },
      "AttributeSpec": {
        "properties": {
          "key": {
            "type": "string",
            "title": "Key"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "group": {
            "$ref": "#/components/schemas/AttributeGroup"
          },
          "order": {
            "type": "integer",
            "title": "Order"
          },
          "format": {
            "$ref": "#/components/schemas/AttributeFormat"
          },
          "unit": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Unit"
              },
              {
                "type": "null"
              }
            ]
          },
          "display": {
            "type": "boolean",
            "title": "Display",
            "default": true
          },
          "derived": {
            "type": "boolean",
            "title": "Derived",
            "default": false
          },
          "prominent": {
            "type": "boolean",
            "title": "Prominent",
            "default": false
          },
          "technical": {
            "type": "boolean",
            "title": "Technical",
            "default": false
          },
          "totalled": {
            "type": "boolean",
            "title": "Totalled",
            "default": false
          },
          "choices": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Choices"
          },
          "fields": {
            "items": {
              "$ref": "#/components/schemas/AttributeSpec"
            },
            "type": "array",
            "title": "Fields",
            "default": []
          }
        },
        "type": "object",
        "required": [
          "key",
          "label",
          "group",
          "order",
          "format"
        ],
        "title": "AttributeSpec",
        "description": "One attribute as declared: label, group, order, format, unit, and any fixed set\nof values it takes."
      },
      "AttributeValue": {
        "properties": {
          "key": {
            "type": "string",
            "title": "Key"
          },
          "value": {
            "$ref": "#/components/schemas/JsonValue"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "group": {
            "$ref": "#/components/schemas/AttributeGroup"
          },
          "order": {
            "type": "integer",
            "title": "Order"
          },
          "format": {
            "$ref": "#/components/schemas/AttributeFormat"
          },
          "unit": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Unit"
              },
              {
                "type": "null"
              }
            ]
          },
          "derived": {
            "type": "boolean",
            "title": "Derived",
            "default": false
          },
          "prominent": {
            "type": "boolean",
            "title": "Prominent",
            "default": false
          },
          "technical": {
            "type": "boolean",
            "title": "Technical",
            "default": false
          },
          "choices": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Choices"
          }
        },
        "type": "object",
        "required": [
          "key",
          "value",
          "label",
          "group",
          "order",
          "format"
        ],
        "title": "AttributeValue",
        "description": "One field of an entity, carrying everything needed to display it without\nrecognising `key`."
      },
      "Block": {
        "properties": {
          "walk": {
            "$ref": "#/components/schemas/Walk"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "group": {
            "$ref": "#/components/schemas/RelationshipGroup"
          },
          "order": {
            "type": "integer",
            "title": "Order"
          },
          "rows": {
            "$ref": "#/components/schemas/RowPage"
          },
          "suppressed": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Suppressed",
            "default": 0
          }
        },
        "type": "object",
        "required": [
          "walk",
          "label",
          "group",
          "order",
          "rows"
        ],
        "title": "Block",
        "description": "One relationship's worth of a page: a label, a first page of rows, the walk\nbehind them, and a count of rows whose target is unpublished."
      },
      "Compared": {
        "properties": {
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "where": {
            "items": {
              "$ref": "#/components/schemas/Condition"
            },
            "type": "array",
            "title": "Where",
            "default": []
          },
          "order": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Ordering"
              },
              {
                "type": "null"
              }
            ]
          },
          "rows": {
            "$ref": "#/components/schemas/RowPage"
          }
        },
        "type": "object",
        "required": [
          "type",
          "rows"
        ],
        "title": "Compared",
        "description": "One page of entities picked out by a number, with the question that picked\nthem."
      },
      "Comparison": {
        "type": "string",
        "enum": [
          "at_least",
          "more_than",
          "at_most",
          "less_than",
          "equals"
        ],
        "title": "Comparison",
        "description": "How a stored number is measured against the one a caller gave."
      },
      "Condition": {
        "properties": {
          "path": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)?$",
            "title": "Path"
          },
          "compare": {
            "$ref": "#/components/schemas/Comparison"
          },
          "value": {
            "type": "number",
            "title": "Value"
          }
        },
        "type": "object",
        "required": [
          "path",
          "compare",
          "value"
        ],
        "title": "Condition",
        "description": "One comparison to hold a stored value against."
      },
      "Direction": {
        "type": "string",
        "enum": [
          "forward",
          "reverse"
        ],
        "title": "Direction",
        "description": "Which way a relationship was read: out of the entity, or back into it."
      },
      "EntityKey": {
        "properties": {
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "id": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Id"
          }
        },
        "type": "object",
        "required": [
          "type",
          "id"
        ],
        "title": "EntityKey",
        "description": "What identifies an entity: its type, plus an id unique only within that type."
      },
      "EntitySummary": {
        "properties": {
          "link": {
            "$ref": "#/components/schemas/Link"
          },
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          }
        },
        "type": "object",
        "required": [
          "link",
          "type"
        ],
        "title": "EntitySummary",
        "description": "One entity as it appears in a list: where it points, and a line about it."
      },
      "EntityType": {
        "type": "string",
        "title": "EntityType",
        "description": "The types of thing this knowledge base holds: items, NPCs, shops and more. Published as an open string rather than a fixed enum, because new values get added as the data grows. The ones this build knows are listed under `x-known-values`. Treat anything else as a value you cannot render yet, not as an error.",
        "x-known-values": [
          "item",
          "npc",
          "shop",
          "quest",
          "location",
          "scenery",
          "task",
          "room",
          "music"
        ],
        "examples": [
          "item"
        ]
      },
      "ErrorBody": {
        "properties": {
          "error": {
            "$ref": "#/components/schemas/ErrorDetail"
          }
        },
        "type": "object",
        "required": [
          "error"
        ],
        "title": "ErrorBody",
        "description": "The envelope every error arrives in: one `error` object, nothing else."
      },
      "ErrorDetail": {
        "properties": {
          "code": {
            "type": "string",
            "title": "Code"
          },
          "message": {
            "type": "string",
            "title": "Message"
          },
          "data_version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Data Version"
          },
          "near_names": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Near Names"
          }
        },
        "type": "object",
        "required": [
          "code",
          "message"
        ],
        "title": "ErrorDetail",
        "description": "What went wrong: a `code` to branch on and a `message` to read.\n\n`data_version` is the build still served when the one asked for is gone;\n`near_names_url` is where to ask what an unmatched name might have meant."
      },
      "GameVersion": {
        "type": "string"
      },
      "GroupPlacement": {
        "type": "string",
        "enum": [
          "infobox",
          "section",
          "hidden"
        ],
        "title": "GroupPlacement",
        "description": "Where the attributes of one group belong: the infobox, a section, or kept in the\ndata but not shown."
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "Health": {
        "properties": {
          "status": {
            "type": "string",
            "const": "ok",
            "title": "Status",
            "default": "ok"
          },
          "data_version": {
            "type": "string",
            "title": "Data Version"
          },
          "schema_version": {
            "type": "integer",
            "title": "Schema Version"
          }
        },
        "type": "object",
        "required": [
          "data_version",
          "schema_version"
        ],
        "title": "Health",
        "description": "The server is answering, and the build it answers from."
      },
      "Hidden": {
        "properties": {
          "outcome": {
            "type": "string",
            "const": "hidden",
            "title": "Outcome",
            "default": "hidden"
          },
          "key": {
            "$ref": "#/components/schemas/EntityKey"
          },
          "reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/HiddenReason"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "key"
        ],
        "title": "Hidden",
        "description": "The entity exists in this build but is deliberately not served."
      },
      "HiddenReason": {
        "type": "string",
        "enum": [
          "unnamed",
          "suppressed",
          "duplicate",
          "placeholder"
        ],
        "title": "HiddenReason",
        "description": "Why an entity is kept in the build but not served to readers."
      },
      "JsonValue": {},
      "Link": {
        "properties": {
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "id": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Id"
          },
          "slug": {
            "type": "string",
            "minLength": 1,
            "title": "Slug"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "icon_ref": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Icon Ref"
          }
        },
        "type": "object",
        "required": [
          "type",
          "id",
          "slug",
          "label"
        ],
        "title": "Link",
        "description": "A pointer to another entity, carrying `type`, `id`, `slug` and `label` but never\na URL."
      },
      "Manifest": {
        "properties": {
          "data_version": {
            "type": "string",
            "minLength": 1,
            "title": "Data Version"
          },
          "schema_version": {
            "type": "integer",
            "minimum": 1.0,
            "title": "Schema Version"
          },
          "content_hash": {
            "type": "string",
            "minLength": 1,
            "title": "Content Hash"
          },
          "built_at": {
            "type": "string",
            "format": "date-time",
            "title": "Built At"
          },
          "game_version": {
            "$ref": "#/components/schemas/GameVersion"
          }
        },
        "type": "object",
        "required": [
          "data_version",
          "schema_version",
          "content_hash",
          "built_at",
          "game_version"
        ],
        "title": "Manifest",
        "description": "What a build says about itself: its data-version, the state of the game behind\nit, and a hash fixing its contents."
      },
      "Match": {
        "properties": {
          "best_match": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Link"
              },
              {
                "type": "null"
              }
            ]
          },
          "results": {
            "$ref": "#/components/schemas/SearchResultPage"
          }
        },
        "type": "object",
        "required": [
          "results"
        ],
        "title": "Match",
        "description": "The entity a name most likely meant, null when nothing matched, with the ranked\ncandidates either way."
      },
      "Missing": {
        "properties": {
          "outcome": {
            "type": "string",
            "const": "missing",
            "title": "Outcome",
            "default": "missing"
          },
          "reference": {
            "type": "string",
            "title": "Reference"
          }
        },
        "type": "object",
        "required": [
          "reference"
        ],
        "title": "Missing",
        "description": "Nothing in this build answers to that reference."
      },
      "Moved": {
        "properties": {
          "outcome": {
            "type": "string",
            "const": "moved",
            "title": "Outcome",
            "default": "moved"
          },
          "target": {
            "$ref": "#/components/schemas/Link"
          }
        },
        "type": "object",
        "required": [
          "target"
        ],
        "title": "Moved",
        "description": "The reference is retired. The entity answers at `target` now."
      },
      "Ordering": {
        "properties": {
          "path": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)?$",
            "title": "Path"
          },
          "descending": {
            "type": "boolean",
            "title": "Descending",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "path"
        ],
        "title": "Ordering",
        "description": "Which declared value to sort by, and which way round."
      },
      "PageDescriptor": {
        "properties": {
          "entity": {
            "$ref": "#/components/schemas/Link"
          },
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "canonical": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Link"
              },
              {
                "type": "null"
              }
            ]
          },
          "variants": {
            "items": {
              "$ref": "#/components/schemas/Link"
            },
            "type": "array",
            "title": "Variants",
            "default": []
          },
          "infobox": {
            "items": {
              "$ref": "#/components/schemas/AttributeValue"
            },
            "type": "array",
            "title": "Infobox",
            "default": []
          },
          "sections": {
            "items": {
              "$ref": "#/components/schemas/Section"
            },
            "type": "array",
            "title": "Sections",
            "default": []
          },
          "blocks": {
            "items": {
              "$ref": "#/components/schemas/Block"
            },
            "type": "array",
            "title": "Blocks",
            "default": []
          },
          "data_version": {
            "type": "string",
            "title": "Data Version"
          }
        },
        "type": "object",
        "required": [
          "entity",
          "type",
          "data_version"
        ],
        "title": "PageDescriptor",
        "description": "A whole entity page as data: identity, a line of description, the infobox, the\nsections, and a first page of each set of related entities."
      },
      "EntitySummaryPage": {
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/EntitySummary"
            },
            "type": "array",
            "title": "Items"
          },
          "total": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Total"
          },
          "limit": {
            "type": "integer",
            "maximum": 200.0,
            "minimum": 1.0,
            "title": "Limit"
          },
          "offset": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Offset"
          },
          "has_more": {
            "type": "boolean",
            "title": "Has More",
            "readOnly": true
          },
          "next_offset": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Offset",
            "readOnly": true
          }
        },
        "type": "object",
        "required": [
          "items",
          "total",
          "limit",
          "offset",
          "has_more",
          "next_offset"
        ],
        "title": "Page[EntitySummary]"
      },
      "PricePointPage": {
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/PricePoint"
            },
            "type": "array",
            "title": "Items"
          },
          "total": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Total"
          },
          "limit": {
            "type": "integer",
            "maximum": 200.0,
            "minimum": 1.0,
            "title": "Limit"
          },
          "offset": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Offset"
          },
          "has_more": {
            "type": "boolean",
            "title": "Has More",
            "readOnly": true
          },
          "next_offset": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Offset",
            "readOnly": true
          }
        },
        "type": "object",
        "required": [
          "items",
          "total",
          "limit",
          "offset",
          "has_more",
          "next_offset"
        ],
        "title": "Page[PricePoint]"
      },
      "RowPage": {
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/Row"
            },
            "type": "array",
            "title": "Items"
          },
          "total": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Total"
          },
          "limit": {
            "type": "integer",
            "maximum": 200.0,
            "minimum": 1.0,
            "title": "Limit"
          },
          "offset": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Offset"
          },
          "has_more": {
            "type": "boolean",
            "title": "Has More",
            "readOnly": true
          },
          "next_offset": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Offset",
            "readOnly": true
          }
        },
        "type": "object",
        "required": [
          "items",
          "total",
          "limit",
          "offset",
          "has_more",
          "next_offset"
        ],
        "title": "Page[Row]"
      },
      "SearchResultPage": {
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/SearchResult"
            },
            "type": "array",
            "title": "Items"
          },
          "total": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Total"
          },
          "limit": {
            "type": "integer",
            "maximum": 200.0,
            "minimum": 1.0,
            "title": "Limit"
          },
          "offset": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Offset"
          },
          "has_more": {
            "type": "boolean",
            "title": "Has More",
            "readOnly": true
          },
          "next_offset": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Offset",
            "readOnly": true
          }
        },
        "type": "object",
        "required": [
          "items",
          "total",
          "limit",
          "offset",
          "has_more",
          "next_offset"
        ],
        "title": "Page[SearchResult]"
      },
      "Present": {
        "properties": {
          "outcome": {
            "type": "string",
            "const": "found",
            "title": "Outcome",
            "default": "found"
          },
          "target": {
            "$ref": "#/components/schemas/Link"
          }
        },
        "type": "object",
        "required": [
          "target"
        ],
        "title": "Present",
        "description": "The reference points at an entity that is published right now."
      },
      "PricePoint": {
        "properties": {
          "item_id": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Item Id"
          },
          "snapshot_date": {
            "type": "string",
            "format": "date",
            "title": "Snapshot Date"
          },
          "value": {
            "type": "integer",
            "minimum": 0.0,
            "title": "Value"
          }
        },
        "type": "object",
        "required": [
          "item_id",
          "snapshot_date",
          "value"
        ],
        "title": "PricePoint",
        "description": "What one item was worth on one snapshot day."
      },
      "RelationshipGroup": {
        "type": "string",
        "title": "RelationshipGroup",
        "description": "Which part of a page the related entities of a relationship belong to. Published as an open string rather than a fixed enum, because new values get added as the data grows. The ones this build knows are listed under `x-known-values`. Treat anything else as a value you cannot render yet, not as an error.",
        "x-known-values": [
          "drops",
          "trade",
          "quests",
          "equipment",
          "map",
          "skill",
          "slayer",
          "prerequisites"
        ],
        "examples": [
          "drops"
        ]
      },
      "RelationshipSpec": {
        "properties": {
          "rel": {
            "$ref": "#/components/schemas/RelationshipType"
          },
          "forward_label": {
            "type": "string",
            "title": "Forward Label"
          },
          "inverse_label": {
            "type": "string",
            "title": "Inverse Label"
          },
          "src_types": {
            "items": {
              "$ref": "#/components/schemas/EntityType"
            },
            "type": "array",
            "uniqueItems": true,
            "title": "Src Types"
          },
          "dst_types": {
            "items": {
              "$ref": "#/components/schemas/EntityType"
            },
            "type": "array",
            "uniqueItems": true,
            "title": "Dst Types"
          },
          "edge_attributes": {
            "items": {
              "$ref": "#/components/schemas/AttributeSpec"
            },
            "type": "array",
            "title": "Edge Attributes"
          },
          "group": {
            "$ref": "#/components/schemas/RelationshipGroup"
          },
          "order": {
            "type": "integer",
            "title": "Order"
          }
        },
        "type": "object",
        "required": [
          "rel",
          "forward_label",
          "inverse_label",
          "src_types",
          "dst_types",
          "edge_attributes",
          "group",
          "order"
        ],
        "title": "RelationshipSpec",
        "description": "One relationship as declared: a label each direction, the types it joins, and\nthe attributes a link may carry."
      },
      "RelationshipType": {
        "type": "string",
        "title": "RelationshipType",
        "description": "The types of link one entity can have to another. Published as an open string rather than a fixed enum, because new values get added as the data grows. The ones this build knows are listed under `x-known-values`. Treat anything else as a value you cannot render yet, not as an error.",
        "x-known-values": [
          "drops",
          "sells",
          "staffed_by",
          "rewards",
          "uses_ammunition",
          "located_in",
          "part_of",
          "yields",
          "makes",
          "requires",
          "assigns",
          "satisfied_by",
          "heard_during"
        ],
        "examples": [
          "drops"
        ]
      },
      "Row": {
        "properties": {
          "link": {
            "$ref": "#/components/schemas/Link"
          },
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "attributes": {
            "items": {
              "$ref": "#/components/schemas/AttributeValue"
            },
            "type": "array",
            "title": "Attributes"
          },
          "about": {
            "items": {
              "$ref": "#/components/schemas/AttributeValue"
            },
            "type": "array",
            "title": "About",
            "default": []
          }
        },
        "type": "object",
        "required": [
          "link",
          "type",
          "attributes"
        ],
        "title": "Row",
        "description": "One entity reached over a relationship, what the link itself records, and the\nfew values the entity at the far end is worth knowing by."
      },
      "SearchResult": {
        "properties": {
          "link": {
            "$ref": "#/components/schemas/Link"
          },
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "score": {
            "type": "number",
            "minimum": 0.0,
            "title": "Score"
          }
        },
        "type": "object",
        "required": [
          "link",
          "type",
          "score"
        ],
        "title": "SearchResult",
        "description": "One entity a search matched; a higher `score` matched better."
      },
      "Section": {
        "properties": {
          "render": {
            "type": "string",
            "title": "Render",
            "default": "attributes"
          },
          "group": {
            "$ref": "#/components/schemas/AttributeGroup"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "placement": {
            "$ref": "#/components/schemas/GroupPlacement"
          },
          "order": {
            "type": "integer",
            "title": "Order"
          },
          "attributes": {
            "items": {
              "$ref": "#/components/schemas/AttributeValue"
            },
            "type": "array",
            "title": "Attributes"
          }
        },
        "type": "object",
        "required": [
          "group",
          "label",
          "placement",
          "order",
          "attributes"
        ],
        "title": "Section",
        "description": "One block of the page body, with a `render` word saying how to lay it out."
      },
      "SortOrder": {
        "type": "string",
        "enum": [
          "name",
          "id"
        ],
        "title": "SortOrder",
        "description": "How a listing is sorted: alphabetically by name, or by id."
      },
      "Tooltip": {
        "properties": {
          "link": {
            "$ref": "#/components/schemas/Link"
          },
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "attributes": {
            "items": {
              "$ref": "#/components/schemas/AttributeValue"
            },
            "type": "array",
            "title": "Attributes",
            "default": []
          }
        },
        "type": "object",
        "required": [
          "link",
          "type"
        ],
        "title": "Tooltip",
        "description": "The short form of an entity: identity, one line, and the few values the\nregistry marks as worth showing."
      },
      "TypeInfo": {
        "properties": {
          "type": {
            "$ref": "#/components/schemas/EntityType"
          },
          "label": {
            "type": "string",
            "title": "Label"
          },
          "plural": {
            "type": "string",
            "title": "Plural"
          },
          "order": {
            "type": "integer",
            "title": "Order"
          },
          "attributes": {
            "items": {
              "$ref": "#/components/schemas/AttributeSpec"
            },
            "type": "array",
            "title": "Attributes"
          },
          "relationships": {
            "items": {
              "$ref": "#/components/schemas/RelationshipSpec"
            },
            "type": "array",
            "title": "Relationships"
          }
        },
        "type": "object",
        "required": [
          "type",
          "label",
          "plural",
          "order",
          "attributes",
          "relationships"
        ],
        "title": "TypeInfo",
        "description": "One entity type, with every attribute and relationship declared for it."
      },
      "Unit": {
        "type": "string",
        "title": "Unit",
        "description": "What a numeric value is measured in, such as kilograms or game ticks. Published as an open string rather than a fixed enum, because new values get added as the data grows. The ones this build knows are listed under `x-known-values`. Treat anything else as a value you cannot render yet, not as an error.",
        "x-known-values": [
          "kg",
          "ticks",
          "tiles"
        ],
        "examples": [
          "kg"
        ]
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "type": "array",
            "title": "Location"
          },
          "msg": {
            "type": "string",
            "title": "Message"
          },
          "type": {
            "type": "string",
            "title": "Error Type"
          },
          "input": {
            "title": "Input"
          },
          "ctx": {
            "type": "object",
            "title": "Context"
          }
        },
        "type": "object",
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError"
      },
      "Walk": {
        "properties": {
          "origin": {
            "$ref": "#/components/schemas/EntityKey"
          },
          "rel": {
            "$ref": "#/components/schemas/RelationshipType"
          },
          "direction": {
            "$ref": "#/components/schemas/Direction"
          }
        },
        "type": "object",
        "required": [
          "origin",
          "rel",
          "direction"
        ],
        "title": "Walk",
        "description": "The question a block answers: which entity, which relationship, which way."
      }
    },
    "securitySchemes": {
      "issued_key": {
        "type": "http",
        "scheme": "bearer",
        "description": "Send the key you were issued as `Authorization: Bearer <token>`. Keys are handed out by whoever runs this deployment and do not expire; one that leaks is withdrawn by its holder's key id. A browser cannot keep one secret, so a public front end should call this through its own backend."
      }
    }
  },
  "tags": [
    {
      "name": "entities",
      "description": "Look up one thing you already know the name or the id of: its page, its hover card, one set of its neighbours, or what an old reference points at now."
    },
    {
      "name": "discovery",
      "description": "Find something when you have no id yet. Browse everything of one type, search the full text, or ask which single entity a name means."
    },
    {
      "name": "meta",
      "description": "About the server rather than the game: whether it can answer, and which build of the data it is answering from."
    }
  ]
}
