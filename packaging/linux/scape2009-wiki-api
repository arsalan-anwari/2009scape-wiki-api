#!/bin/sh
#
# The one command every other name is a shortcut for: serve, mcp, keys, data.

: "${WIKI_API_CONFIG_DIR:=/etc/scape2009-wiki-api}"
export WIKI_API_CONFIG_DIR

exec /usr/lib/scape2009-wiki-api/scape2009-wiki-api "$@"
