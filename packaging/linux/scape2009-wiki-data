#!/bin/sh
#
# Fetch a newer published dataset than the one the package installed, or say which
# build this machine is answering from. 

: "${WIKI_API_CONFIG_DIR:=/etc/scape2009-wiki-api}"
export WIKI_API_CONFIG_DIR

exec /usr/lib/scape2009-wiki-api/scape2009-wiki-api data "$@"
